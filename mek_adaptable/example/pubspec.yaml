name: example
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  mek_adaptable: ^0.0.0

dependency_overrides:
  mek_adaptable:
    path: ..

dev_dependencies:
  lints: ^2.0.0

  build_runner: ^2.1.11
  mek_json_adapter:
    path: ../../mek_json_adapter
